import { Routes } from '@angular/router';

import { DiscoverComponent } from './discover.component';

export const DISCOVERY_ROUTES: Routes = [
  { path: '', component: DiscoverComponent, title: 'Find artists · OpenMicHub' },
];
