# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database + vector search
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
pgvector==0.3.6

# Embeddings. fastembed runs ONNX rather than pulling in torch, which keeps the
# image around 1GB instead of 4GB.
fastembed==0.5.1

# Ranking
lightgbm==4.5.0
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.1
joblib==1.4.2

# Tests. Installed in the image so the suite runs in the same interpreter and
# against the same versions the service runs on:
#     docker compose exec ml python -m pytest tests -q
pytest==8.3.4
